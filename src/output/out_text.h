#ifndef NM_OUT_TEXT_H
#define NM_OUT_TEXT_H

#include "core/graph.h"

/* Print network map as text table to stdout. Returns 0 on success. */
int nm_out_text(const nm_graph_t *g);

#endif /* NM_OUT_TEXT_H */
